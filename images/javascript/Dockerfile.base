# syntax=docker/dockerfile:1.6
FROM node:20.9-alpine

LABEL maintainer="whanos" \
      org.whanos.language="javascript"

WORKDIR /workspace

# Default command executes the repository's package main via `node .`.
CMD ["node", "."]
