# syntax=docker/dockerfile:1.6
FROM python:3.12-slim

LABEL maintainer="whanos" \
      org.whanos.language="python"

ENV PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /workspace

# Requirements are installed by derived images; base image only provides the runtime.
CMD ["python", "-m", "app"]
