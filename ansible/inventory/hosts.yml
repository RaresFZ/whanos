all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
  children:
    jenkins:
      hosts:
        localhost:
    registry:
      hosts:
        localhost:
    k8s_control_plane:
      hosts:
        localhost:
    k8s_workers:
      hosts:
        localhost:
